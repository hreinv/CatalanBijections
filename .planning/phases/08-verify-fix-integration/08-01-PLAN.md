---
phase: 08-verify-fix-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-vertical-slice/02-VERIFICATION.md
  - .planning/phases/03-first-bijections/03-VERIFICATION.md
autonomous: true
requirements:
  - STRC-01
  - STRC-02
  - STRC-06
  - STRC-10
  - STRC-12
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04
  - ANIM-05
  - ANIM-06
  - UICT-01
  - UICT-02
  - UICT-03
  - UICT-04
  - UICT-05
  - UICT-06
  - UICT-07
  - BIJC-01
  - BIJC-02
  - BIJC-03

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 2 confirming all 14 requirements pass their success criteria"
    - "VERIFICATION.md exists for Phase 3 confirming all 7 requirements pass their success criteria"
    - "All 21 previously-partial requirements now pass the 3-source cross-reference (VERIFICATION + SUMMARY + REQUIREMENTS.md)"
    - "Each verification document independently confirms claims by examining actual source files, not just restating SUMMARY claims"
  artifacts:
    - path: ".planning/phases/02-vertical-slice/02-VERIFICATION.md"
      provides: "Phase 2 formal verification report covering 14 requirements"
      contains: "status: passed"
    - path: ".planning/phases/03-first-bijections/03-VERIFICATION.md"
      provides: "Phase 3 formal verification report covering 7 requirements"
      contains: "status: passed"
  key_links:
    - from: ".planning/phases/02-vertical-slice/02-VERIFICATION.md"
      to: "REQUIREMENTS.md traceability table"
      via: "3-source cross-reference"
      pattern: "STRC-01.*SATISFIED"
    - from: ".planning/phases/03-first-bijections/03-VERIFICATION.md"
      to: "REQUIREMENTS.md traceability table"
      via: "3-source cross-reference"
      pattern: "BIJC-01.*SATISFIED"
---

<objective>
Create formal VERIFICATION.md documents for Phase 2 (Vertical Slice) and Phase 3 (First Bijections), independently confirming that all 21 previously-partial requirements are satisfied with evidence from actual source code.

Purpose: The v1.0 Milestone Audit found 21 requirements marked "partial" because the work was done and claimed complete in SUMMARYs, but no VERIFICATION.md documents exist. This plan closes that documentation gap so the 3-source cross-reference passes for all completed work.

Output: Two VERIFICATION.md files following the established template from Phase 1's 01-VERIFICATION.md.
</objective>

<execution_context>
@/home/hvanover/.claude/get-shit-done/workflows/execute-plan.md
@/home/hvanover/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-VERIFICATION.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-verify-fix-integration/08-RESEARCH.md
@.planning/phases/02-vertical-slice/02-01-PLAN.md
@.planning/phases/02-vertical-slice/02-01-SUMMARY.md
@.planning/phases/02-vertical-slice/02-02-PLAN.md
@.planning/phases/02-vertical-slice/02-02-SUMMARY.md
@.planning/phases/02-vertical-slice/02-03-PLAN.md
@.planning/phases/02-vertical-slice/02-03-SUMMARY.md
@.planning/phases/03-first-bijections/03-01-PLAN.md
@.planning/phases/03-first-bijections/03-01-SUMMARY.md
@.planning/phases/03-first-bijections/03-02-PLAN.md
@.planning/phases/03-first-bijections/03-02-SUMMARY.md
@.planning/phases/03-first-bijections/03-03-PLAN.md
@.planning/phases/03-first-bijections/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 2 VERIFICATION.md (14 requirements)</name>
  <files>.planning/phases/02-vertical-slice/02-VERIFICATION.md</files>
  <action>
Create `.planning/phases/02-vertical-slice/02-VERIFICATION.md` following the EXACT template structure from `.planning/phases/01-core-foundation/01-VERIFICATION.md`. This is a VERIFICATION document, not a restatement of SUMMARY claims.

**CRITICAL: Independently verify by examining actual source files.** Do NOT copy claims from SUMMARY files. For each requirement, grep/read the actual JS source and confirm the implementation exists with file paths and line numbers.

**Frontmatter:**
```yaml
---
phase: 02-vertical-slice
verified: 2026-02-23T00:00:00Z
status: passed
score: 14/14 must-haves verified
re_verification: false
---
```

**Sections to include (matching Phase 1 template):**

1. **Goal Achievement** -- Observable Truths table from ROADMAP Phase 2 success criteria (5 items):
   - All 4 structures render correctly for C(n) instances at n=1..4
   - Each structure's toDyck/fromDyck round-trips for all 14 instances at n=4
   - Presenter can select any two structures from dropdowns, side-by-side
   - Playback controls respond correctly, animation via rAF
   - Instance navigator cycles through all C(n) instances with X of Y indicator
   For each: set Status = VERIFIED and provide Evidence by examining actual source files (file names, line numbers, function signatures, grep patterns).

2. **Required Artifacts** -- Sub-tables for Plan 02-01 (6 artifacts), Plan 02-02 (3 artifacts), Plan 02-03 (2 artifacts). For each artifact: confirm the file exists, note line count, confirm it exports the expected functions/constants, list key implementation details found by reading the actual file.

3. **Key Link Verification** -- For each plan's key_links from the PLAN frontmatter, use grep to confirm the import/wiring pattern exists in the actual source. Record the grep command, matched line, and status.

4. **Requirements Coverage** -- Final table mapping each of the 14 requirement IDs (STRC-01, STRC-02, STRC-06, STRC-10, STRC-12, ANIM-01..ANIM-05, UICT-01..UICT-03, UICT-05) to their satisfaction evidence. Each must show Source Plan, Description, Status=SATISFIED, and specific evidence from code inspection.

5. **Anti-Patterns Found** -- Scan all Phase 2 source files for TODO/FIXME/HACK/PLACEHOLDER/stubs. Note the dead import in binary-triang.js and orphaned THEME_COLORS/lerp as known items being fixed in Plan 08-02 (do not mark them as blockers for Phase 2 verification).

6. **Notable Deviations** -- Document any differences between plan intent and actual implementation that do not affect success criteria (e.g., toDyck() never called at runtime but tested in harness -- still satisfies STRC-12 per requirement wording "round-trip identity verified").

7. **Human Verification Required** -- List items that need browser testing (structure rendering visual checks, UI control interaction, animation playback smoothness).

8. **Commit Verification** -- Check git log for commits claimed in the 3 SUMMARY files and confirm they exist.

9. **Summary** -- Concise conclusion stating Phase 2 goal is achieved with evidence.

**Source files to inspect (read actual content, do not guess):**
- `js/structures/parentheses.js` -- STRC-01
- `js/structures/dyck-path.js` -- STRC-02
- `js/structures/binary-tree.js` -- STRC-06
- `js/structures/triangulation.js` -- STRC-10
- `js/structures/registry.js` -- all STRC
- `tests/test-structures.html` -- STRC-12
- `index.html` -- UICT-01, UICT-02, UICT-03, UICT-05
- `css/style.css` -- UICT-05
- `js/main.js` -- all UICT, all ANIM wiring
- `js/engine/animation.js` -- ANIM-01..ANIM-05
  </action>
  <verify>
    <automated>grep -c "SATISFIED" .planning/phases/02-vertical-slice/02-VERIFICATION.md | grep -q "14" && grep -q "status: passed" .planning/phases/02-vertical-slice/02-VERIFICATION.md && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify the 02-VERIFICATION.md has all 10 sections matching the Phase 1 template and contains independent evidence (file paths, line numbers, grep results) rather than just restating SUMMARY claims</manual>
  </verify>
  <done>02-VERIFICATION.md exists with status:passed, all 14 requirements listed as SATISFIED with independent source code evidence, and the document follows the Phase 1 template structure</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 3 VERIFICATION.md (7 requirements)</name>
  <files>.planning/phases/03-first-bijections/03-VERIFICATION.md</files>
  <action>
Create `.planning/phases/03-first-bijections/03-VERIFICATION.md` following the EXACT template structure from `.planning/phases/01-core-foundation/01-VERIFICATION.md`. This is a VERIFICATION document, not a restatement of SUMMARY claims.

**CRITICAL: Independently verify by examining actual source files.** Do NOT copy claims from SUMMARY files.

**Frontmatter:**
```yaml
---
phase: 03-first-bijections
verified: 2026-02-23T00:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---
```

**Sections to include (matching Phase 1 template):**

1. **Goal Achievement** -- Observable Truths table from ROADMAP Phase 3 success criteria (5 items):
   - Parens-to-Dyck-Paths animates with 5+ distinct steps, each with text description
   - Binary-Trees-to-Triangulations animates showing node-triangle correspondence
   - Parens-to-Binary-Trees animates showing nesting-subtree mapping
   - Matching elements share the same color across source and target
   - Active transformation step pulses/glows while non-active elements are dimmed
   For each: set Status = VERIFIED and provide Evidence from actual source file inspection.

2. **Required Artifacts** -- Sub-tables for Plan 03-01 (4 artifacts), Plan 03-02 (2 artifacts), Plan 03-03 (3 artifacts). Confirm each file exists, note line count, confirm exports and key implementation details.

3. **Key Link Verification** -- For each plan's key_links, use grep to confirm the wiring pattern exists. Record grep command, matched line, status.

4. **Requirements Coverage** -- Final table mapping each of the 7 requirement IDs (BIJC-01, BIJC-02, BIJC-03, ANIM-06, UICT-04, UICT-06, UICT-07) to satisfaction evidence.

5. **Anti-Patterns Found** -- Scan all Phase 3 source files. Note the dead import in binary-triang.js as a known item being fixed in Plan 08-02.

6. **Notable Deviations** -- Document any non-blocking differences (e.g., lerp() was planned as an artifact but is unused and being removed in Plan 08-02; the dead triangulation import in binary-triang.js).

7. **Human Verification Required** -- List items needing browser testing (bijection animation visual checks, color correspondence, glow/dim effects).

8. **Commit Verification** -- Check git log for commits from the 3 SUMMARY files.

9. **Summary** -- Concise conclusion.

**Source files to inspect (read actual content):**
- `js/core/easing.js` -- ANIM-06
- `js/bijections/router.js` -- all BIJC
- `js/bijections/parens-dyck.js` -- BIJC-01, UICT-06, UICT-07
- `js/bijections/binary-triang.js` -- BIJC-02
- `js/bijections/parens-binary.js` -- BIJC-03
- `js/main.js` -- UICT-04 (step description panel wiring), render mode switching
- `index.html` -- UICT-04 (step-description DOM element)
- `css/style.css` -- step description panel styling
  </action>
  <verify>
    <automated>grep -c "SATISFIED" .planning/phases/03-first-bijections/03-VERIFICATION.md | grep -q "7" && grep -q "status: passed" .planning/phases/03-first-bijections/03-VERIFICATION.md && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify the 03-VERIFICATION.md has all sections matching the Phase 1 template and contains independent evidence rather than restating SUMMARY claims</manual>
  </verify>
  <done>03-VERIFICATION.md exists with status:passed, all 7 requirements listed as SATISFIED with independent source code evidence, and the document follows the Phase 1 template structure</done>
</task>

</tasks>

<verification>
1. Both VERIFICATION.md files exist and have valid YAML frontmatter with `status: passed`
2. 02-VERIFICATION.md contains exactly 14 SATISFIED requirement entries
3. 03-VERIFICATION.md contains exactly 7 SATISFIED requirement entries
4. Both files follow the 01-VERIFICATION.md template structure (Goal Achievement, Required Artifacts, Key Links, Requirements Coverage, Anti-Patterns, Notable Deviations, Human Verification, Commit Verification, Summary)
5. Evidence in both files references actual source file paths and line numbers, not just SUMMARY restatements
6. The 3-source cross-reference now passes for all 21 requirements: VERIFICATION confirms + SUMMARY claims + REQUIREMENTS.md marks [x]
</verification>

<success_criteria>
- `.planning/phases/02-vertical-slice/02-VERIFICATION.md` exists with 14/14 requirements SATISFIED
- `.planning/phases/03-first-bijections/03-VERIFICATION.md` exists with 7/7 requirements SATISFIED
- Both documents independently verify claims with source file evidence (grep patterns, line numbers, function signatures)
- Both documents follow the established Phase 1 verification template exactly
</success_criteria>

<output>
After completion, create `.planning/phases/08-verify-fix-integration/08-01-SUMMARY.md`
</output>
