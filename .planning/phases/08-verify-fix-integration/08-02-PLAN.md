---
phase: 08-verify-fix-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/bijections/binary-triang.js
  - js/core/colors.js
  - js/core/easing.js
autonomous: true
requirements:
  - STRC-01
  - STRC-02
  - STRC-06
  - STRC-10
  - STRC-12
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04
  - ANIM-05
  - ANIM-06
  - UICT-01
  - UICT-02
  - UICT-03
  - UICT-04
  - UICT-05
  - UICT-06
  - UICT-07
  - BIJC-01
  - BIJC-02
  - BIJC-03

must_haves:
  truths:
    - "Dead import of triangulation module in binary-triang.js is removed"
    - "Orphaned THEME_COLORS export in colors.js is removed"
    - "Orphaned lerp() function in easing.js is removed"
    - "validate() in dyck.js is NOT removed (it is used internally by enumerate())"
    - "The app still loads and runs correctly after all removals (no functional changes)"
  artifacts:
    - path: "js/bijections/binary-triang.js"
      provides: "Binary Trees to Triangulations bijection without dead triangulation import"
    - path: "js/core/colors.js"
      provides: "Color palette with only CORRESPONDENCE_COLORS export"
    - path: "js/core/easing.js"
      provides: "Easing utilities with only easeInOutCubic export"
  key_links:
    - from: "js/bijections/binary-triang.js"
      to: "js/structures/binary-tree.js"
      via: "ES module import"
      pattern: "import.*binaryTree.*from.*binary-tree"
    - from: "js/core/colors.js"
      to: "js/main.js"
      via: "CORRESPONDENCE_COLORS import"
      pattern: "import.*CORRESPONDENCE_COLORS.*from.*colors"
    - from: "js/core/easing.js"
      to: "js/main.js"
      via: "easeInOutCubic import"
      pattern: "import.*easeInOutCubic.*from.*easing"
---

<objective>
Apply three surgical code fixes identified by the v1.0 Milestone Audit: remove a dead import, an orphaned export, and an orphaned utility function. These are purely subtractive changes with zero functional impact.

Purpose: Clean up tech debt items (dead import in binary-triang.js, orphaned THEME_COLORS in colors.js, orphaned lerp() in easing.js) so the audit passes with no integration issues.

Output: Three modified source files with dead code removed.
</objective>

<execution_context>
@/home/hvanover/.claude/get-shit-done/workflows/execute-plan.md
@/home/hvanover/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verify-fix-integration/08-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead import and orphaned exports</name>
  <files>
    js/bijections/binary-triang.js
    js/core/colors.js
    js/core/easing.js
  </files>
  <action>
Apply three surgical code fixes. Each is a pure deletion -- no new code added, no behavior changed.

**Fix 1: Dead import in binary-triang.js**
- Open `js/bijections/binary-triang.js`
- Delete line 15: `import * as triangulation from '../structures/triangulation.js';`
- Keep line 14: `import * as binaryTree from '../structures/binary-tree.js';` (this IS used)
- Verify: grep the file to confirm no reference to `triangulation.` exists (the module implements all triangulation drawing locally with its own functions like `drawPolygonOutline`, `drawTrianglesWithZones`, etc.)

**Fix 2: Orphaned THEME_COLORS in colors.js**
- Open `js/core/colors.js`
- Delete the JSDoc comment `/** Theme colors for UI elements */` (line 23)
- Delete the entire `export const THEME_COLORS = { ... };` block (lines 24-30)
- Keep everything above: the module-level JSDoc comment (lines 1-9), the CORRESPONDENCE_COLORS JSDoc (line 11), and the CORRESPONDENCE_COLORS export (lines 12-21)
- Also update the module-level JSDoc to remove the line mentioning THEME_COLORS: change "THEME_COLORS: UI chrome colors for backgrounds, strokes, and grid lines." to remove that line since the export no longer exists

**Fix 3: Orphaned lerp() in easing.js**
- Open `js/core/easing.js`
- Delete the JSDoc comment for lerp (lines 24-30)
- Delete the `export function lerp(a, b, t) { ... }` block (lines 31-34, or wherever it starts after the JSDoc)
- Actually the JSDoc is lines 24-30 and the function is lines 31-33 based on the file read. Delete lines 24-34 (everything after easeInOutCubic).
- Keep everything above: module JSDoc (lines 1-7), easeInOutCubic JSDoc (lines 9-15), and easeInOutCubic function (lines 16-22, ending with the closing brace)

**DO NOT remove validate() from dyck.js.** The audit listed it as orphaned, but it IS used internally by enumerate() on line 84 for self-verification and is tested in test-dyck.html. Removing it would break the self-verification assertions.

After all three fixes, verify no import of the removed items exists anywhere in the codebase:
- `grep -r "THEME_COLORS" js/` should return nothing
- `grep -r "import.*lerp" js/` should return nothing (bijection modules define their own local lerp functions)
- `grep -r "triangulation" js/bijections/binary-triang.js` should return nothing (except possibly in comments about the bijection itself)
  </action>
  <verify>
    <automated>grep -r "THEME_COLORS" js/ && echo "FAIL: THEME_COLORS still exists" || echo "PASS: THEME_COLORS removed"; grep "import.*triangulation" js/bijections/binary-triang.js && echo "FAIL: dead import still exists" || echo "PASS: dead import removed"; grep "export function lerp" js/core/easing.js && echo "FAIL: lerp still exported" || echo "PASS: lerp removed"; grep "export function validate" js/core/dyck.js && echo "PASS: validate kept" || echo "FAIL: validate was incorrectly removed"</automated>
    <manual>Open http://localhost:8080/ in a browser and confirm the app loads without console errors. Select a bijection pair (e.g., Binary Trees / Triangulations) and verify animation still plays correctly.</manual>
  </verify>
  <done>
    - binary-triang.js no longer imports triangulation module (dead import removed)
    - colors.js exports only CORRESPONDENCE_COLORS (THEME_COLORS removed)
    - easing.js exports only easeInOutCubic (lerp removed)
    - validate() in dyck.js is untouched
    - App loads and bijection animations play correctly (no functional regression)
  </done>
</task>

</tasks>

<verification>
1. `grep -r "THEME_COLORS" js/` returns no matches
2. `grep "import.*triangulation" js/bijections/binary-triang.js` returns no matches
3. `grep "export function lerp" js/core/easing.js` returns no matches
4. `grep "export function validate" js/core/dyck.js` returns a match (validate is correctly preserved)
5. `grep "export function easeInOutCubic" js/core/easing.js` returns a match (easeInOutCubic preserved)
6. `grep "CORRESPONDENCE_COLORS" js/core/colors.js` returns a match (CORRESPONDENCE_COLORS preserved)
7. App loads in browser without console errors
</verification>

<success_criteria>
- Three dead/orphaned code items removed from three source files
- validate() in dyck.js is preserved (NOT removed)
- No functional regression -- app loads and runs identically
- All three audit integration issues resolved
</success_criteria>

<output>
After completion, create `.planning/phases/08-verify-fix-integration/08-02-SUMMARY.md`
</output>
